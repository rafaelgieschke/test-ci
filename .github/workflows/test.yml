name: learn-github-actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -la
      - run: echo a > a
      - run: git add a; GIT_AUTHOR_NAME=bot GIT_AUTHOR_EMAIL=bot@invalid.invalid git commit -m x
      - run: git push
